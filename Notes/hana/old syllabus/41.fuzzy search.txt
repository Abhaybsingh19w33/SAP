1)Create table ZGT _FUZZY
create column table ZGT_FUZZY(
ID integer,
TXT varchar(20));


2) Insert records
insert into ZGT_FUZZY values(1,'hello');
insert into ZGT_FUZZY values(3,'hell');
insert into ZGT_FUZZY values(4,'hel');
insert into ZGT_FUZZY values(5,'ello');
insert into ZGT_FUZZY values(7,'hello world');
insert into ZGT_FUZZY values(8,'hell world');
insert into ZGT_FUZZY values(14,'helloworld');
insert into ZGT_FUZZY values(15,'hellworld');
insert into ZGT_FUZZY values(16,'HelloWorld');
insert into ZGT_FUZZY values(17,'HELLO');
insert into ZGT_FUZZY values(21,'world');
insert into ZGT_FUZZY values(22,'word');


3) write sql query
SELECT  * FROM ZGT_FUZZY
WHERE CONTAINS(txt, 'Hello', FUZZY(0.8))


SELECT  * FROM ZGT_FUZZY
WHERE CONTAINS(txt, 'Hello', FUZZY(0.3))




to_decimal (Score(),3,2) means 3 digits  out of which two will be the decimal value.


SELECT TO_DECIMAL(SCORE(),3,2) AS score, * FROM ZGT_FUZZY
WHERE CONTAINS(txt, 'Hello', FUZZY(0.8))
ORDER BY score DESC;

4) SELECT TO_DECIMAL(SCORE(),3,2) AS score, * FROM ZGT_FUZZY
WHERE CONTAINS(txt, 'Hello', FUZZY(0.3))
ORDER BY score DESC;


As the  fuzzy score changes,notice the  output in 3 and 4. 

Score 0 is  Least match
Score 1 is Exact Match









