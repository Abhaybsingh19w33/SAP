*&---------------------------------------------------------------------*
*& Report ZRT_OPTIMISED12_MOVECORES
*&---------------------------------------------------------------------*
*&
*&---------------------------------------------------------------------*
report z.

SELECT *
       FROM SFLIGHT
       INTO TABLE @DATA(SFLIGHT_TAB).

LOOP AT SFLIGHT_TAB INTO DATA(WA)
                  GROUP BY ( Key1 = WA-CARRID key2 = WA-CONNID ).
  WRITE :/ WA-CARRID, WA-CONNID .
ENDLOOP.