REPORT Z NO STANDARD PAGE HEADING.
PARAMETERS:PEMPNO(4)  TYPE N,
           PENAME(10) TYPE C,
           PSAL       TYPE P DECIMALS 2,
           PCOMM      TYPE P DECIMALS 2.

AT SELECTION-SCREEN OUTPUT.
  LOOP AT SCREEN.
    IF SCREEN-NAME = 'PSAL' OR SCREEN-NAME = 'PCOMM'.
      SCREEN-REQUIRED = 1.
      MODIFY SCREEN.
    ENDIF.
  ENDLOOP.

START-OF-SELECTION.
  WRITE :/ 'Ename  is :', PENAME.
  WRITE :/ 'Empno  is :', PEMPNO.
  WRITE :/ 'Salary is:', PSAL.
  WRITE :/ 'Comm   is :', PCOMM.