REPORT Z.
"Reading record from internal table
SELECT
  CARRID,
  CONNID,
  CITYFROM,
  CITYTO
  FROM SPFLI
  INTO TABLE @DATA(ITAB).

"READ TABLE ITAB INTO DATA(WA) WITH KEY CARRID = 'AA' CONNID = '017'.
"WRITE : / WA-CARRID, WA-CONNID.
"CLEAR WA.


*optimised abap
DATA(WA) = ITAB[ CARRID = 'AA' CONNID = '017' ].
WRITE : / WA-CARRID, WA-CONNID.