REPORT Z.
SELECT EMPNO,SAL,
  CASE
  WHEN SAL >= 4000  THEN 'High'
  WHEN SAL < 4000 AND SAL >= 3000 THEN 'Medium'
  WHEN SAL <= 3000 THEN 'Low'

  END AS SAL_RANGE
  FROM Z104329EMP
  INTO TABLE @DATA(LT_RESULT).

CL_DEMO_OUTPUT=>DISPLAY( LT_RESULT ).