REPORT Z.
"NEW METHOD
DATA LV_BONUSPERC TYPE P DECIMALS 2 VALUE '0.8'.
SELECT ( 1 + 1 ) AS TWO,
  ( @LV_BONUSPERC * SAL )  AS BONUS,
  CEIL( COMM ) AS CEILED_COMM
  FROM Z104329EMP
  INTO TABLE @DATA(LT_RESULT).

CL_DEMO_OUTPUT=>DISPLAY( LT_RESULT ).