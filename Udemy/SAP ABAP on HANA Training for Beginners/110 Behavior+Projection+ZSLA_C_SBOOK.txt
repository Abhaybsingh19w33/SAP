projection;
strict ( 2 );

define behavior for ZSLA_C_SBOOK //alias <alias_name>
{
  use create;
  use update;
  use delete;
// Expose the actions in the BO behavior projection.
// Actions defined in base behavior definitions must be included in the projection behavior definition
// if you want to expose it for an OData service.
  use action set_status;
}